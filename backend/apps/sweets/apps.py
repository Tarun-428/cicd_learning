from django.apps import AppConfig

class SweetsConfig(AppConfig):
    """
    Configuration class for the sweets application.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.sweets"
